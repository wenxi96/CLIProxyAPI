Review Status
- workflow.operation.name: P02-backend-verification-review
- workflow.operation.status: completed
- workflow.review_scope.status: complete
- workflow.scope_check.status: requirements_missing
- workflow.findings.status: findings_reported
- verdict: changes_requested

Review Scope

只读审查了指定候选文件、`go.mod`、本地 `AGENTS.md`、现有 focused tests 和当前分支/远端基线。未修改文件，未运行安装、合并、提交、推送、部署或长测试。

Scope Check

范围控制基本正确：计划明确后端-only、`dev <- origin/main`、不 push/tag/release/deploy，且任务章程把 `master` 推进放在验证后再评估。

主要问题是验证计划还不足以证明“吸收成功且未覆盖 fork 定制”。`go test ./...` 和 build 是必要底线，但当前 focused checks 没有被写成可执行命令，也缺少能证明 OAuth alias 与 scoped-pool 合并顺序正确的组合测试要求；Home plugin status 的必需证据路径在当前仓库不存在。

Findings

ID: P02-F1
Severity: High
Summary: OAuth alias 与 fork scoped-pool 的组合行为没有明确验证闭环。
Evidence: `findings.md:51-53` 把 `sdk/cliproxy/auth/conductor.go` 的关键策略定为“alias candidate 生成后仍执行 scoped-pool filter”；现有测试只分散覆盖 alias 与 scoped-pool，例如 `conductor_oauth_alias_suspension_test.go:66`、`scoped_pool_test.go:28`。`rg` 未发现同时覆盖 OAuth alias candidate + scoped-pool filter 的测试。
Impact: L03 合并若改变筛选顺序，可能出现 alias 路由绕过 scoped-pool、错误候选被选中，或 fork 的池化隔离被上游 alias force-mapping 覆盖；全量 build/test 不一定能定位该行为回归。
Recommendation: 在 L03/L04 计划中加入必须通过的组合测试要求，至少覆盖：两个同 provider OAuth auth、route model 通过 alias 映射到 upstream model、scoped-pool limit/penalty 生效、最终只选中 scoped-pool 允许的 auth，且 executor 收到 upstream model、usage context 保留 requested alias。
Confidence: High

ID: P02-F2
Severity: Medium
Summary: Home plugin sync/reporting 验证引用了不存在或未落地的证据文件。
Evidence: Required Evidence 要求 `internal/home/plugin_status_test.go`，但当前仓库不存在该文件；`rg --files internal | rg 'plugin.*status.*test|status.*plugin.*test|home.*test'` 只找到 `internal/homeplugins/sync_test.go` 等文件。当前 `cmd/server/main.go:312-315` 仍是合并前的 `applyHomeRuntimeDefaults` + `homeplugins.Sync`，而计划期望吸收 `SyncWithReport` / `ReportPluginStatus`。
Impact: L04 可能只验证插件下载/安装，不验证上游新增 sync report/status reporting 是否与 fork runtime defaults 同时保留。
Recommendation: 计划中明确合并后的断言：Home 配置路径必须先执行 `applyHomeRuntimeDefaults`，再调用上游 report-aware sync，并将结果提交给 Home status reporting。若上游测试文件会由 merge 带入，L03 后必须确认文件存在并跑对应 focused test；否则新增等价测试。
Confidence: High

ID: P02-F3
Severity: Medium
Summary: 验证命令存在，但 focused test 命令和 Docker 可用性前置检查不够明确。
Evidence: 计划只列出 `go test ./...`、`go build -buildvcs=false -o /tmp/cli-proxy-api-check ./cmd/server` 和泛称“聚焦测试”见 implementation plan `:35-38`、`:79-89`；`findings.md:57-59` 给了 Docker Go 1.26 命令，但没有说明本地 Go/Docker image 不可用时在 L03 前停止还是 L04 停止。
Impact: 如果主机没有 Go 且 Docker 需要 pull 或不可用，可能在合并后才发现无法验证，增加回退/停滞成本。
Recommendation: 在 L03 开始前加入只读环境预检：`go version` 或 `docker image inspect golang:1.26` / 明确可用的 Docker runner；L04 写出 focused test 命令清单，再跑全量测试和 build。
Confidence: Medium

Scorecard
- Scope Control: 4
- Evidence Quality: 2
- Correctness: 3
- Safety: 4
- Testability: 2
- Maintainability: 3

Verification Evidence

- `go.mod:3` 要求 Go `1.26.0`，因此 Docker Go 1.26 fallback 合理。
- 当前分支核验：`dev@3359d754a390`，`origin/main@4c0c60292d27`，`upstream/main@4c0c60292d27`，与 `findings.md` 基线一致。
- 当前工作区已有 `.agents/README.md` 修改和新任务目录，未做任何写入。
- 现有 focused tests 读取范围包括 xAI executor、OAuth alias、API key alias、OAuth alias suspension、scoped-pool、Home plugin sync、server Home defaults。
- 未运行 `go test ./...` 或 build，因为本 packet 明确禁止 long-running full tests 且本轮是 plan review。

Open Questions / Limitations

- 未读取外部 upstream PR/commit 内容；只基于当前本地 mirror refs、计划文档和现有测试审查。
- `internal/home/plugin_status_test.go` 可能是上游合并后才会出现的文件；当前计划没有把“合并后确认该测试存在或补等价测试”写成停止/通过条件。
- 没有执行 Docker 或 Go 环境探测，按 packet 只做只读轻量审查。

Recommended Next Step

先更新 L03/L04 计划，不要直接进入代码合并。最低修正是补上 focused test 命令清单、OAuth alias + scoped-pool 组合测试要求、Home plugin report/status 的合并后断言，以及 Go/Docker 验证环境预检和不可用时停止条件。修完后再允许 L03 合并。

