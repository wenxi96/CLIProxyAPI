Review Status
- workflow.operation.name: plan_review
- workflow.operation.status: completed_with_limitations
- workflow.review_scope.status: partial
- workflow.scope_check.status: mixed
- workflow.findings.status: findings_reported
- verdict: changes_requested

Review Scope

Base: `dev@3359d754a390`; candidate upstream: `origin/main@4c0c60292d27`; merge base: `bd646819ed95`. Reviewed the requested task docs, local `AGENTS.md` / `CLAUDE.md`, required commit list, and the three stated conflict files on `dev` vs `origin/main`.

Scope Check

No files were modified. I did not run merge/commit/push/tag/release/deploy. The required `git merge-tree --write-tree --name-only dev origin/main` could not complete in this read-only sandbox because Git attempted to create temporary/object DB files, so the exact conflict list is not independently confirmed in this session.

Findings

ID: F-01
Severity: High
Summary: Fork customization preservation is under-specified outside the three text-conflict files.
Evidence: The plan promises preserving scoped-pool, quota auto-disable, usage persistence, and external auth lifecycle, but lists only three code files plus “merge 自动修改文件” and focused tests for xAI/OAuth/pluginhost/conductor (`plans/...implementation-plan.md:3`, `:24-38`, `:72-88`). Merge-base comparison shows fork-only preservation files such as `sdk/cliproxy/auth/scoped_pool.go`, `quota_check_async.go`, `internal/authquota/service.go`, `sdk/cliproxy/service_external_auth_registration_test.go`, and `service_usage_persistence_test.go`; `origin/main` lacks key fork hooks like `authMaintenanceHook` and scoped-pool scheduler/types.
Impact: A clean text merge can still disconnect or silently drop fork-only behavior.
Recommendation: Add a fork-preservation checklist with post-merge file/symbol checks and targeted tests for scoped-pool, quota auto-disable, usage persistence, and external auth lifecycle.
Confidence: High

ID: F-02
Severity: Medium
Summary: `cmd/server/main.go` strategy misses upstream’s second plugin status phase.
Evidence: Findings only require `SyncWithReport` plus `ReportPluginStatus` (`findings.md:51`). Upstream also calls `homeplugins.MarkLoadResults` after `pluginHost.ApplyConfig` and reports load status (`origin/main:cmd/server/main.go:571-583`).
Impact: Home plugin sync may report fetch status but miss load-result reporting, weakening upstream plugin management behavior.
Recommendation: Update the conflict strategy to preserve both phases: sync report during Home fetch and load-result reporting after plugin runtime apply, while keeping fork runtime defaults.
Confidence: High

ID: F-03
Severity: Medium
Summary: `xai_executor.go` strategy is too narrow for the upstream replay/sanitizer changes.
Evidence: Findings mention only the empty `reasoning` object check and “保留 fork 请求清洗入口” (`findings.md:52`). Upstream adds `applyXAIReasoningReplayCacheRequired`, `sanitizeXAIInputEncryptedContent`, replay scope on prepared requests, and completion-event cache writes (`origin/main:internal/runtime/executor/xai_executor.go:841-849`, `:867`, grep evidence at `:185` and `:669`).
Impact: Implementer could resolve the visible conflict while omitting Grok reasoning replay or invalid encrypted-content sanitization.
Recommendation: Specify the exact call order and add focused xAI tests for reasoning replay plus encrypted_content sanitization.
Confidence: High

ID: F-04
Severity: Medium
Summary: `conductor.go` scoped-pool vs alias ordering is ambiguous.
Evidence: Findings say scoped-pool filter should still execute “在 alias candidate 生成后” (`findings.md:53`). In `dev`, scoped-pool filters auth candidates before selector selection (`dev:sdk/cliproxy/auth/conductor.go:4248`, `:4419`); in `origin/main`, alias candidates/rewrite happen per selected auth and response (`origin/main:sdk/cliproxy/auth/conductor.go:2510`, `:2554`, `:5137`, `:5188`).
Impact: A wrong resolution can let out-of-pool auths be selected, or can skip alias response rewriting on some paths.
Recommendation: Rewrite this strategy as an invariant: scoped-pool filters auth candidate sets before scheduler/default selection; alias candidate/rewrite stays per selected auth across normal, stream, and credits paths. Add combined scoped-pool + force-mapping regression tests.
Confidence: Medium

Scorecard
- Scope Control: 3
- Evidence Quality: 3
- Correctness: 2
- Safety: 3
- Testability: 3
- Maintainability: 3

Verification Evidence

- `git log --reverse --oneline dev..origin/main` returned 28 commits, ending at `4c0c6029`, matching the matrix count.
- `git rev-parse` confirmed `dev == origin/dev == 3359d754a390` and `origin/main == upstream/main == 4c0c60292d27`.
- `git merge-tree --write-tree --name-only dev origin/main` failed: `Read-only file system`, `unable to add sdk/cliproxy/service.go to database`.
- Merge-base overlap check found both-side changes beyond the three conflict files, including `sdk/cliproxy/service.go`, `sdk/cliproxy/auth/types.go`, config/store files, and xAI/conductor/main.
- No runtime tests were run; this was a read-only plan review.

Open Questions / Limitations

- Exact merge conflict list still needs confirmation in a writable isolated worktree or normal execution surface.
- I did not fetch remotes or verify whether upstream drifted beyond local refs.
- I did not inspect every changed file in the 275-file diff; review focused on the stated plan, required files, and fork-customization risk paths.

Recommended Next Step

Do not enter L03 code merge yet. First update `findings.md` / implementation plan to close F-01 through F-04, then rerun `merge-tree` or an isolated merge rehearsal in a writable worktree and attach that evidence before approving code merge.
