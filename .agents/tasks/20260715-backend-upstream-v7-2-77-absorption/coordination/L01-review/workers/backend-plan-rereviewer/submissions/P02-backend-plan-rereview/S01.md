# 后端方案独立复评 S01

- reviewer: Darwin / 019f6536-5788-7271-820d-cc022110bee2
- mode: read-only
- status: changes_requested
- files_touched: none

## Disposition 核验

- H-01 Usage v2: fixed。
- H-02 43-path ledger: fixed，43 条无重复并与 Git 重叠路径一致。
- H-03 kind/scoped pool: fixed。
- H-04 risk-to-proof: fixed。
- H-05 dev/master: reopen，新的 candidate `.agents` 检查对象错误。
- M-01 hunk 策略: fixed。
- M-02 跨域分类: fixed。
- M-03 401/quota/pool: fixed。

## R2-H-01 Master 的 .agents 门禁检查了旧 HEAD

- Severity: High。
- Evidence: governance plan 在提交 candidate 前执行 `git ls-tree -r HEAD -- .agents`；此时 HEAD 仍是旧 master，不是待提交 index/candidate tree。
- Impact: 旧 master 本来无 `.agents`，即使待提交 index 含 `.agents` 也会错误通过；后续业务树比较又排除了 `.agents`。
- Recommendation: 提交前执行 `git ls-files --stage -- .agents`；提交后执行 `git ls-tree -r --name-only "$master_candidate_sha" -- .agents`，两者均为空。
- Disposition: fixed。

## 结论

- 新增 finding: High 1。
- Verdict: changes_requested。
- 除 R2-H-01 外无新的 high/medium finding。
- 修订后需执行聚焦 Round 3 只读复核。
